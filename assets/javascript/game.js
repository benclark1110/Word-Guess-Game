var movies = ["pulp fictopn", "django unchained", "the avengers", "the hateful eight", "shaft"];

var randomMovie = movies[Math.floor(Math.random() * movies.length)];
console.log(randomMovie);

/*Not sure aboout below*/
var movieSpacesElem = document.getElementById("movieSpaces");
